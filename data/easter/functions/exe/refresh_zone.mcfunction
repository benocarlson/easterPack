# Run this command from a command block defining a range for the hunting area

# The command block should have the following command:
# execute as @a[<define distance/range here>] run function easter:general/refresh_zone

scoreboard players set @s in_zone 420